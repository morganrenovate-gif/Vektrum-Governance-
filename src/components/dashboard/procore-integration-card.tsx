'use client'
import Link from 'next/link'
import { useEffect, useState } from 'react'
import { Boxes } from 'lucide-react'
export function ProcoreIntegrationCard(){const [connected,setConnected]=useState(false);useEffect(()=>{fetch('/api/integrations/procore/status').then(r=>r.json()).then(d=>setConnected(d.connection?.status==='connected')).catch(()=>undefined)},[]);return <section className="rounded-xl border border-vektrum-blue/25 bg-vektrum-blue/[0.06] p-5 shadow-card" aria-label="Procore Development Sandbox"><div className="flex items-start justify-between gap-4"><div className="flex gap-3"><div className="flex h-9 w-9 items-center justify-center rounded-lg bg-vektrum-blue/20"><Boxes size={17} className="text-blue-300" aria-hidden="true"/></div><div><p className="text-[14px] font-semibold text-white">Procore Development Sandbox</p><p className="mt-1 text-[12px] leading-relaxed text-white/65">Connect a Procore sandbox account to bring verified project context into Vektrum.</p><p className="mt-2 text-[11px] font-semibold uppercase tracking-wide text-blue-300">{connected?'Connected':'Not connected'} · Read-only</p></div></div><Link href="/dashboard/funder/integrations/procore" className="shrink-0 rounded-lg bg-vektrum-blue px-3 py-2 text-[12px] font-semibold text-white hover:bg-vektrum-blue-hover">{connected?'Manage Connection':'Connect Procore'}</Link></div></section>}
